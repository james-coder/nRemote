package com.ti.eps.navnet;
public class NavNet {
    public static int connect(NodeHandle h, int port, ConnectionHandle ch) { return 0; }
    public static int write(ConnectionHandle ch, byte[] data, int len) { return 0; }
    public static int disconnect(ConnectionHandle ch) { return 0; }
}
